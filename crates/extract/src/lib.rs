//! Extract SysML v2 models to YAML/JSON for code generation.
//!
//! Bridges the architecture layer (SysML v2) to the generation contract
//! layer (custom YAML/JSON), producing normalized module/FSM/interface
//! descriptors consumable by the code generator.
